# 待确认事项

已发现、但还不能拍板的数据问题。确认后回来改这里，并同步 `docs/coordinates.md`。

## 1. 孔位数（暂不处理，先记录）

来源：`docs/reference/yuquan-charging-points-map.json`（校园地图《玉泉充电桩点位明细》的提取结果，用户于 2026-09-18 提供）。

**目前不能当成孔位数用**：该文件自带说明「`map_marked_number` 为原图地点名称后括号中的数字；原图未明确单位，因此不要自动解释为充电桩台数或充电口数」。所以先原样记录，只做对照。

名称无歧义的 10 个站点（名称对不上的 4 个见第 3 节）：

| 站点 | 地图标注 | 我们（设备数 × 12） | 差 |
| --- | --- | --- | --- |
| 高分子系北侧 | 60 | 60（5 台） | 0 |
| 曹光彪楼南侧 | 50 | 48（4 台） | +2 |
| 生仪楼南侧 | 60 | 48（4 台） | **+12** |
| 原电工厂 | 44 | 48（4 台） | −4 |
| 教十八西侧 | 17 | 24（2 台） | −7 |
| 教三北侧 | 10 | 12（1 台） | −2 |
| 教十（内部） | 9 | 12（1 台） | −3 |
| 建发宿舍（内部） | 16 | 24（2 台） | −8 |
| 出版社楼南侧 | 16 | 24（2 台） | −8 |
| 教七北侧 | 20 | 36（3 台） | **−16** |
| **小计** | **302** | **336** | |

全部 14 站：地图合计 **408**，我们设备算出 **420**。

第 3 节又确定了 3 个名称，补进来后可以完全对账（只剩「智泉大楼 / 科工楼东侧」这一对无法对照）：

| 地图名 | 地图 | 我们 | 差 |
| --- | --- | --- | --- |
| 微电子学院 → 行政楼北侧 | 25 | 24（2 台） | +1 |
| 机电工程楼 → 电机楼南侧 | 16 | 24（2 台） | −8 |
| 内单 → 新桥门南侧 | 35 | 24（2 台） | +11 |

除这 1 对以外：地图 **378** vs 我们 **408**（差 −30，即地图普遍比我们少）。

**怎么才能确认**：现场数一次插座最直接；或找上游是否有表示孔位的字段（我们目前只用到 `portstatur` 的字符个数）。若地图标注确实是孔数，差异集中在两处、可作起点：`生仪楼南侧` 地图比我们多 **12**（正好一台 12 孔设备，疑似我们少登记一台）、`教七北侧` 地图比我们少 16。

## 2. 校园地图文件不能当作坐标使用（已核对）

`docs/reference/yuquan-charging-points-map.json` 里**没有每个点的经纬度**——只有 960×609 底图上的像素锚点 `image_anchor.pixel`，外加一个「校区级」参考点（文件自己也注明 `not a coordinate for each charging point`）。

更关键的是：这张底图**不是按比例绘制的示意图**，所以像素锚点换算不出可用坐标。

证据（用我们已核对的站点坐标作基准配对）：

- 取 10 个名称无歧义的站点，共 45 对，`像素距离 / 实际距离` 落在 **0.73 ~ 3.37 米/像素**，相差 **4.6 倍**；用全部 14 站则是 0.41 ~ 5.57，相差 13.7 倍。地图若按比例，这个比值应当恒定。
- 单应（透视）模型并不比仿射好（平均残差 104.9 m vs 103.2 m），说明不是「斜着拍的照片」，而是示意图。
- 即便强行拟合，14 点平均残差约 **105 m**、最大约 **350 m**，明显差于我们现有坐标（约 45 m）。
- 方位角也对不上：例如「教十 → 原电工厂」图上 68.8°、实际 12.2°，差 56.6°。

**结论：这个「像素锚点版」不能用来改站点坐标。** 我们的坐标来自上游接口，精度更好。

### 2.1 带绝对坐标的版本（2026-09-18 追加核对）

`yuquan_charging_points_absolute_coordinates.json`（schema 2.0）确实带了逐点绝对坐标，但生成方式分两类：

| 生成方式 | 点位数 | 标称精度 | 说明 |
| --- | --- | --- | --- |
| `direct_building_centroid` | 5 | 15 m | 直接取 OSM/Mapcarta 的**建筑中心**（教七、高分子楼、教三、教十、曹光彪楼） |
| `affine_georeferencing` | 8 | 40 m | 由上面那张**非比例示意图**仿射配准推算 |

核对结果：

- **坐标系标注是对的**：其 `wgs84` 与 `gcj02` 的互相换算与标准算法一致（14 点最大偏差 **0.10 m**），可以安全换算。这点很关键——上一个版本就是因为坐标系来源不明才用不了。
- **5 个「建筑中心」点是真实位置，但它是楼心、不是桩位**：与我们按上游登记的位置相差 **21~72 m**，差值方向正好是「桩在楼的哪一侧」（曹光彪楼南侧 49 m、高分子系北侧 72 m）。拿来导航到桩位反而更差。
- **8 个「仿射」点继承了示意图的畸变**：与我们相差 **24~175 m**（原出版社 175 m、建发宿舍 103 m、生仪学院 92 m），远大于其自称的 ±40 m，该精度声明偏乐观。
- 10 个名称明确点位合计：平均差 **66 m**、最大 **175 m**。因此**不用它替换 `stations_yuquan.csv`**：现有坐标来自上游设备登记，已用 OSM 独立校核（约 45 m），更适合导航。
- 它能提供的是：站点名称对应（见第 3 节）、孔位数（第 1 节），以及一次独立交叉验证——没有任何一点与现有坐标出现方向性矛盾。

文件已存档为 `docs/reference/yuquan-charging-points-absolute.json`。

## 3. 站点名称对应：4 个里 3 个已确定，「智泉大楼」仍存疑

用 10 个名称已确认的点位标定一个换算（该标定自身不确定度约 ±39 m），再看这 4 个点「应该」落在哪个候选站点上：

| 地图名 | 最可能对应 | 标定后距离 | 次佳 | 判定 |
| --- | --- | --- | --- | --- |
| 微电子学院 | 玉泉行政楼北侧 | **27 m** | 新桥门南侧 239 m | 可靠 |
| 机电工程楼 | 玉泉电机楼南侧 | **52 m** | 科工楼东侧 125 m | 可靠（OSM 同名建筑为「电机工程楼（电气工程学院）」，距该站 51 m） |
| 内单 | 玉泉新桥门南侧 | **85 m** | 行政楼北侧 154 m | 较可靠（该点距 OSM「新桥门」57 m） |
| 智泉大楼 | ✗ 无匹配 | 行政楼北侧 405 m、科工楼东侧 594 m | — | 存疑 |

「智泉大楼」的问题：它到任何一个剩余候选站点的距离都在 **405 m 以上**，远超标定不确定度；OSM 里也只有**智泉路**（一条路），没有叫「智泉大楼」的建筑。因此它可能是**识别错的标签**，也可能这张图上有一点是我们清单里没有的。

由此还剩一个未定：`玉泉科工楼东侧` 是否对应地图上的某一点（其 OSM 邻域是美居酒店 / 民宿 / 车辆所，不算典型教学区）。需要看底图确认。

**已落地**：上表前 3 行已作为搜索别名写入 `stations_yuquan.csv` 的 `aliases` 列（面板搜索框可直接搜「微电子学院」「内单」「机电工程楼」），其余楼宇叫法（第七/第三/第十教学大楼、高分子楼、生仪学院、原出版社等）也一并加上。若底图确认了「智泉大楼」，再补上最后一条。

## 4. 该参考点的坐标系是 GCJ-02（不是我们用的 BD-09）

文件里唯一的经纬度是 `source.campus_reference_coordinate` = `120.122946, 30.263776`。

- 与「14 个站点质心」的 **GCJ-02** 表示相差 **144 m**
- 与同一质心的 **BD-09** / **WGS-84** 表示分别相差 **821 m** / **831 m**

所以它是 **GCJ-02**（多半是从高德类地图上读下来的）。这意味着同一个文件家族里混着两种坐标系：点位数据要用 BD-09，这个参考点是 GCJ-02，**混用会偏约 930 m**。目前该参考点没有被代码使用。

## 5. 位置上报：已做轻量版，聚合版暂缓

2026-09-18 决定：先做「本地自检 + 报错入口」，暂不做用户上报聚合。

**已实现（`static/index.html`）**

- 定位后每张站点卡显示「你当前位置距本站约 xx 米」；若你在某站点 200 米内，页面直接提示你似乎就在哪个站点旁边。
- 站点卡提供「登记位置有误？上报」，点开是预填好的 GitHub issue：含站点在 BD-09 / GCJ-02 / WGS-84 下的登记值、本次定位（BD-09 与浏览器原始 WGS-84）、定位精度、时间。
- 位置不落库、页面不自行发送；**只有用户主动点链接才会发出，且 issue 是公开的**。

**为什么暂缓「匿名上报 + 后端聚合」**

- 站点坐标已准到约 45 米，剩余不确定只有生仪楼一站，一次现场核对即可解决。
- 精度账不划算：手机 GPS 在楼宇间误差常 30~100 米，要把定位压到 5 米量级每站需几十次上报；且用户标记的是自己站位（常在楼门口）而非桩位，存在系统性偏移，样本再多也不会消失。
- 代价：需引入数据库（Vercel KV 等）、修改隐私说明、加防滥用与人工审核。

**什么时候值得做完整版**：同一站点累积到一定量彼此一致的上报时（例如 ≥10 条），再评估，并同步更新隐私说明。
